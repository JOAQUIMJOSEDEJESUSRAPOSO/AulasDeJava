# Copilando
- Para compilar o meu codigo Java eu preciso escrever no terminal o comando ``javac nomeDoArquivo.java``.
- vai gerar um arquivo compilado ``nomeDoArquivo.class`` que é o bytecode.
- Depois basta escrever no terminal ``java nomeDoArquivo`` que ele irá rodar.
# Interface

- Define um contrato que as classes devem seguir.

- ão pode ser instanciada.

- Os métodos não possuem implementação (apenas a assinatura).

- A classe que implementa a interface é obrigada a implementar todos os métodos definidos nela.

- Serve para padronizar comportamentos entre classes diferentes.

👉 Em resumo: a interface diz o que deve ser feito, mas não como será feito.

# Abstração (Classe Abstrata)

- Não pode ser instanciada.

- Pode conter:

- Métodos abstratos (sem implementação)

- Métodos concretos (com implementação)

- As classes que herdam uma classe abstrata devem implementar apenas os métodos abstratos.

- Permite compartilhar código e lógica comum entre classes.

👉 Em resumo: a classe abstrata pode definir o que deve ser feito e também parte de como será feito.